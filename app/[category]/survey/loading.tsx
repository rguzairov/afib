import { SurveyLoading } from "@/components/features/disease-elements/survey-loading";

export default function Loading() {
  return (
    <SurveyLoading
      title="Loading survey"
      description="Pick the entries that fit you. Tap Yes, No, or Skip — and add your own at the end."
    />
  );
}
